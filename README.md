To run this locally, you need to have [Python 3](https://www.python.org/downloads/) installed.

Then, run the following commands in the terminal:

```bash
pip install -r requirements.txt
python debug.py
```
